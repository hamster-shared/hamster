use crate::{mock::*, Error};
use frame_support::{assert_noop, assert_ok};
use sp_hamster::p_market::{MarketUserStatus, UserInfo};

#[test]
fn test_crate_market_account() {
	new_test_ext().execute_with(|| {
		// Dispatch a signed extrinsic.
		
	});
}
